// Json word files. 
import en from './locale-data/en.json';
import no from './locale-data/no.json';

// Sets dictionary list to the imported json files.
export const dictionaryList = { en, no };

// Defines the to language options.
export const languageOptions = {
  en: 'English',
  no: 'Norsk'
};